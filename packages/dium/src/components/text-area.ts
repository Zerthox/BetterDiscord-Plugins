import * as Finder from "../api/finder";
import {ForwardRefExoticComponent, MemoExoticComponent} from "../react-internals";

export const ChannelTextArea: MemoExoticComponent<ForwardRefExoticComponent<any>> = Finder.bySource(["pendingReply"]);
